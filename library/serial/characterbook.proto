syntax = "proto2";

package dnd;

message character{
    // for now KISS and characters are just names
    // soon add other attributes of characters
    optional string name;
}

message characterleger{
    repeated character characters;
}